name: Publish to npm

on:
  push:
    tags:
      - 'v*'

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          # Must satisfy package.json engines (>=22.5.0): the worker uses
          # node:sqlite, which is unavailable on Node 20 and made the
          # smoke:clean-room worker-boot check exit 1.
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'
      - name: Install Bun (required by smoke:clean-room)
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: npm install --ignore-scripts
      - run: npm run build
      - run: npm run smoke:clean-room
      - run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
